        <nav class="navbar navbar-inverse navbar-fixed-top">
    	  <div class="container">
		    <!-- Brand and toggle get grouped for better mobile display -->
		    <div class="navbar-header">
		      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-brand-centered">
		        <span class="sr-only">Toggle navigation</span>
		        <span class="icon-bar"></span>
		        <span class="icon-bar"></span>
		        <span class="icon-bar"></span>
		      </button>
		      <div class="navbar-brand navbar-brand-centered"><b>teendahan</b></div>
		    </div>

		    <!-- Collect the nav links, forms, and other content for toggling -->
		    <div class="collapse navbar-collapse" id="navbar-brand-centered">
		      
		     <% if admin_signed_in? %>
		      <ul class="nav navbar-nav">
		        <li><a href="#">Report</a></li>
		        <li><%= link_to "Products", products_path %></li>
		        <li><%= link_to "Cashiers", users_path %></li>
		      </ul>
		      <ul class="nav navbar-nav navbar-right">
		        <li><a href="#">Logged in as <strong><%= current_admin.name %></strong></a></li>
		        <li><%= link_to "Logout", destroy_admin_session_path, method: :delete %></li>
		      </ul>
		     <% end %>
		     
		    <% if user_signed_in? %>
		      <ul class="nav navbar-nav navbar-right">
		        <li><a href="#">Logged in as <strong><%= current_user.name %></strong></a></li>
		        <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
		      </ul>
		     <% end %>
		     
		    </div><!-- /.navbar-collapse -->
		  </div><!-- /.container-fluid -->
		</nav>